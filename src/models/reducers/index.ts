import { combineReducers } from 'redux';
import { State, Action } from '@Models';
import {} from './root';
import {} from './async';

export /*Types*/{};
export const allReducers = combineReducers<State, Action>({});
